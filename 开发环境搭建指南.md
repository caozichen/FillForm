# FillForm 开发环境搭建与运行指南

本文面向第一次接触 Git、Node.js 和 Chrome 扩展开发的读者。按照本文操作后，你可以在一台全新的 Windows 或 macOS 电脑上：

1. 下载 FillForm 源代码。
2. 安装项目依赖。
3. 运行检查脚本并构建扩展。
4. 在 Google Chrome 中加载并使用 FillForm。
5. 修改源码后重新构建、重新加载和打包扩展。

> 注意：FillForm 是 Chrome 扩展，不是需要打开 `localhost` 的普通网站，也不需要启动后端服务。这里的“运行项目”是指构建扩展，然后让 Chrome 加载扩展目录。

## 1. 先选择你要做什么

| 目标 | 是否需要 Node.js/npm | 应该怎么做 |
| --- | --- | --- |
| 只使用当前已构建好的扩展 | 不需要 | 下载或克隆仓库后，直接在 Chrome 中加载 `extension/chrome-mv3`。 |
| 修改源码、调试或重新构建 | 需要 | 完整执行本文对应操作系统的环境安装、`npm ci` 和 `npm run build`。 |
| 把扩展带到其他电脑 | 视情况而定 | 可以复制 `extension/chrome-mv3`，或执行 `npm run zip` 后复制 ZIP；Chrome 加载前必须先解压 ZIP。 |

仓库会提交已经构建好的 `extension/chrome-mv3`，所以单纯使用扩展的人不必安装开发依赖。但是，要保证这个目录包含自己刚修改的代码，必须先执行 `npm run build`。

## 2. 初学者需要知道的几个词

- **终端**：输入命令的窗口。Windows 中本文使用 PowerShell，macOS 中使用“终端”应用。
- **项目根目录**：直接包含 `package.json`、`package-lock.json` 和 `wxt.config.ts` 的 `FillForm` 文件夹。
- **依赖**：项目使用的第三方程序包。它们会安装到 `node_modules`，不用逐个手动下载。
- **构建**：把项目源码转换成 Chrome 可以加载的扩展文件。
- **未打包扩展**：一个已经包含 `manifest.json` 的普通文件夹。Chrome 的“加载已解压的扩展程序”需要选择这个文件夹。
- **命令提示符**：终端中光标前面的路径或符号。只输入代码块里的命令，不要输入示例中的提示符，也不要输入 Markdown 的三个反引号。

除非特别说明，本文命令都应在项目根目录执行。每输入一条命令后按 Enter，等待它执行完成并重新出现输入光标，再执行下一条。

## 3. 环境和版本要求

### 3.1 必需软件

| 软件 | 项目要求 | 本文推荐 | 用途 |
| --- | --- | --- | --- |
| 操作系统 | 仓库未声明最低版本 | Windows 10/11 64 位，或仍受 Chrome 支持的 macOS | 运行开发工具和 Chrome。 |
| Google Chrome | 完整功能建议 114 或更高 | 最新稳定版 | 加载 Manifest V3 扩展；项目使用 `sidePanel` 和 `offscreen` API。 |
| Node.js | 当前锁定依赖至少需要 `22.13.0` | Node.js 22 LTS；本项目已用 `22.23.2` 验证 | 运行 WXT、构建脚本和检查脚本。 |
| npm | 建议 10.x | `10.9.8` 已验证 | 根据锁文件安装依赖；安装 Node.js 时会一并安装。 |
| Git | 任意近期 2.x | 最新稳定的 Git 2.x | 从 GitHub 下载和更新代码。 |
| VS Code | 非必需 | 最新稳定版 | 编辑源码，对初学者更方便。 |

Node.js 版本是本项目最容易踩坑的地方。虽然部分直接依赖可以运行在较旧的 Node 上，但当前 `package-lock.json` 中的 `listr2 10.2.2` 要求 Node.js `22.13.0` 或更高，浏览器回归脚本也使用 Node.js 22 提供的全局 `WebSocket`。因此，本文统一使用 Node.js 22 LTS，不建议使用 Node.js 18、20、21 或低于 `22.13.0` 的 22.x 版本。

### 3.2 项目直接依赖的实际版本

下面的“声明范围”来自 `package.json`，“当前锁定版本”来自 `package-lock.json`：

| 类型 | 依赖 | 声明范围 | 当前锁定版本 | 用途 |
| --- | --- | --- | --- | --- |
| 开发依赖 | `wxt` | `^0.20.27` | `0.20.27` | Chrome 扩展开发、开发服务器、构建和打包。 |
| 开发依赖 | `typescript` | `^5.5.4` | `5.9.3` | TypeScript 编译和类型支持。 |
| 运行依赖 | `hashids` | `^2.3.0` | `2.3.0` | Hashids 编码和解码。 |
| 运行依赖 | `jsqr` | `^1.4.0` | `1.4.0` | 本地二维码识别。 |
| 运行依赖 | `zxing-wasm` | `^3.1.0` | `3.1.0` | 本地条形码和二维码 WASM 解码。 |

当前锁文件还间接安装了 `Vite 8.1.0` 等构建依赖。你不需要逐个安装表格中的包，也不要全局安装 WXT 或 TypeScript。项目根目录执行一次 `npm ci`，npm 就会按照 `package-lock.json` 安装精确版本。

### 3.3 不需要安装的东西

本项目的安装、构建和默认填充功能不需要：

- Python、Java、Docker、数据库或本地后端服务。
- 全局安装 `wxt`、`typescript` 或其他 npm 包。
- `.env` 文件或项目专用环境变量。
- DeepSeek、OpenAI 或智谱 API Key。默认的 `heuristic` 模式完全在本地生成测试数据。

安装依赖和第一次克隆代码时需要访问互联网。测试在线表单或使用可选的云端模型时，也需要访问对应网站或接口。

## 4. Windows 10/11 从零搭建

以下步骤使用 Windows 自带的 PowerShell。建议把项目放在当前用户的 `source` 文件夹中，避免系统目录权限和 OneDrive 同步造成干扰。

### 4.1 安装 Google Chrome

1. 打开 [Google Chrome 官方下载页](https://www.google.com/chrome/)。
2. 下载并安装最新稳定版 Chrome。
3. 安装完成后，在 Chrome 地址栏输入 `chrome://version`。
4. 确认版本不低于 114。通常直接安装最新版即可。

### 4.2 安装 Git for Windows

1. 打开 [Git for Windows 官方下载页](https://git-scm.com/download/win)。
2. 下载与系统架构匹配的安装程序。普通 Intel/AMD 电脑选择 x64，Windows on ARM 电脑选择 ARM64。
3. 初次安装可以保留默认选项。涉及命令行 PATH 时，保留允许从命令行使用 Git 的默认选项。
4. 安装完成后，关闭之前打开的 PowerShell 窗口。

### 4.3 安装 Node.js 22 LTS

1. 打开 [Node.js 官方下载页](https://nodejs.org/en/download)。如果首页不再显示 Node.js 22，请进入 [Node.js Previous Releases](https://nodejs.org/en/about/previous-releases) 查找 22.x。
2. 选择 Node.js 22 LTS，版本必须是 `22.13.0` 或更高；为了复现当前验证环境，可以选择 `22.23.2`。
3. 选择 `.msi` 安装程序。普通 Intel/AMD 电脑选择 x64，Windows on ARM 电脑选择 ARM64。
4. 运行安装程序，保留 npm 和“添加到 PATH”等默认选项。
5. 安装完成后，关闭所有 PowerShell 窗口再重新打开，使 PATH 生效。

Node.js 安装程序会同时安装 npm，不需要再单独下载 npm。

### 4.4 安装代码编辑器（可选）

推荐从 [Visual Studio Code 官方网站](https://code.visualstudio.com/) 安装 VS Code。只想构建和使用现有代码时可以跳过这一步。

### 4.5 打开 PowerShell 并验证环境

1. 点击“开始”菜单。
2. 输入 `PowerShell`。
3. 打开“Windows PowerShell”或新版“PowerShell”。不需要“以管理员身份运行”。
4. 逐条执行：

```powershell
git --version
node --version
npm --version
```

一个符合本文环境的示例结果是：

```text
git version 2.x.x
v22.23.2
10.9.8
```

小版本不必完全相同，但 Node.js 必须是 `v22.13.0` 或更高。若提示“不是内部或外部命令”或“无法将 ... 识别为 cmdlet”，先关闭并重新打开 PowerShell；仍然失败时，重新检查对应软件是否安装并加入 PATH。

### 4.6 克隆项目

只需在一台新电脑上克隆一次。逐条执行：

```powershell
New-Item -ItemType Directory -Force "$HOME\source" | Out-Null
Set-Location "$HOME\source"
git clone https://github.com/caozichen/FillForm.git
Set-Location .\FillForm
```

这些命令会把项目放到类似下面的位置：

```text
C:\Users\你的用户名\source\FillForm
```

确认当前位于正确的项目根目录：

```powershell
Get-Location
Test-Path .\package.json
Test-Path .\package-lock.json
```

后两条命令都应返回 `True`。如果项目已经存在，不要再次执行 `git clone`，直接使用 `Set-Location` 进入现有的 `FillForm` 文件夹。

### 4.7 安装依赖

确保 PowerShell 当前路径是 `FillForm`，然后执行：

```powershell
npm ci
```

`npm ci` 会完成以下工作：

- 严格按照 `package-lock.json` 安装当前项目验证过的版本。
- 在根目录创建 `node_modules`。
- 自动运行 `postinstall` 中的 `wxt prepare`，生成 `.wxt` 类型文件。

安装时间取决于网络速度。成功时命令会正常结束并重新出现 PowerShell 输入光标，不应有红色的 `npm ERR!`。不要把其他电脑，尤其是 macOS 电脑的 `node_modules` 复制到 Windows；其中可能包含与操作系统相关的二进制文件。

可以这样确认安装结果：

```powershell
Test-Path .\node_modules
Test-Path .\.wxt\tsconfig.json
```

两条命令都应返回 `True`。

### 4.8 运行基础检查

先运行两个不需要启动浏览器的检查：

```powershell
npm run check:detect
npm run check:hashids
```

命令没有出现错误、最终回到 PowerShell 输入光标，就表示基础识别逻辑和 Hashids 功能通过检查。项目当前没有统一的 `npm test` 命令。

### 4.9 构建扩展

执行：

```powershell
npm run build
```

该命令先让 WXT 生成生产产物 `.output\chrome-mv3`，再运行同步脚本，刷新稳定扩展目录 `extension\chrome-mv3`。成功时终端末尾应能看到类似内容：

```text
Synced unpacked extension to extension/chrome-mv3
```

验证 Chrome 要加载的目录：

```powershell
Test-Path .\extension\chrome-mv3\manifest.json
Resolve-Path .\extension\chrome-mv3
```

第一条应返回 `True`。第二条会显示该文件夹的完整地址，例如：

```text
C:\Users\你的用户名\source\FillForm\extension\chrome-mv3
```

### 4.10 在 Chrome 中加载扩展

1. 打开 Google Chrome。
2. 在地址栏输入 `chrome://extensions` 并按 Enter。
3. 打开页面右上角的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 在文件夹选择窗口中进入 PowerShell 的 `Resolve-Path` 所显示的地址。
6. 选择 `chrome-mv3` 文件夹并确认。
7. 扩展列表中出现“FillForm — 一键填充与二维码助手”，且没有红色错误，即表示加载成功。

必须选择直接包含 `manifest.json` 的 `extension\chrome-mv3`，不能选择下面这些目标：

- 不能选择项目根目录 `FillForm`。
- 不能只选择父目录 `extension`。
- 不能直接选择 ZIP 文件。
- 日常加载不需要选择临时目录 `.output\chrome-mv3`。

### 4.11 第一次验证插件

1. 打开一个普通的、允许扩展运行的表单网页。
2. 点击 Chrome 工具栏右侧的扩展图标，找到 FillForm，并可点击图钉固定。
3. 打开 FillForm 弹窗，尝试“识别字段”或“一键智能填充”。
4. 默认数据来源是本地 `heuristic`，不配置 API Key 也能运行。

Chrome 内置页面、Chrome 网上应用店、新标签页及其他禁止内容脚本注入的页面不能用于测试表单填充。这是 Chrome 的安全限制，不是安装失败。

### 4.12 Windows 开发模式

需要边修改边调试时，在项目根目录执行：

```powershell
npm run dev
```

WXT 会生成 `.output\chrome-mv3-dev`，通常会打开一个独立的 Chrome/Chromium 开发窗口，并监听文件变化。运行期间 PowerShell 会一直被占用，这是正常现象；按 `Ctrl+C` 停止。

`npm run dev` 不会刷新仓库中的 `extension\chrome-mv3`。如果你在自己日常使用的 Chrome 中加载的是 `extension\chrome-mv3`，完成源码修改后仍应执行 `npm run build`，然后在 `chrome://extensions` 中点击 FillForm 卡片上的“重新加载”，最后刷新正在测试的网页。

### 4.13 Windows 更新到最新版代码

以后更新代码时，不要再次克隆。打开 PowerShell 后执行：

```powershell
Set-Location "$HOME\source\FillForm"
git pull
npm ci
npm run build
```

然后回到 `chrome://extensions`，点击 FillForm 的“重新加载”，再刷新目标网页。如果本地有尚未提交的修改，`git pull` 可能提示冲突；应先提交或妥善保存自己的修改，不要强行删除。

## 5. macOS 从零搭建

以下步骤使用 macOS 自带的“终端”。示例把项目放在当前用户的 `~/Developer` 文件夹中。

### 5.1 安装 Google Chrome

1. 打开 [Google Chrome 官方下载页](https://www.google.com/chrome/)。
2. 下载适合当前 Mac 的最新版 Chrome 并安装到“应用程序”。
3. 打开 Chrome，在地址栏输入 `chrome://version`。
4. 确认版本不低于 114。通常直接安装最新版即可。

Apple Silicon 和 Intel Mac 都可以运行本项目。下载 Chrome 和 Node.js 时，Apple Silicon 选择 ARM64，Intel 处理器选择 x64；官方网站通常会自动推荐正确版本。

### 5.2 打开终端并安装 Git

1. 按 `Command + 空格` 打开聚焦搜索。
2. 输入“终端”并打开。
3. 先检查 Git：

```bash
git --version
```

如果已经显示 `git version 2.x.x`，直接继续下一节。如果系统提示需要开发者工具，执行：

```bash
xcode-select --install
```

在系统弹窗中点击“安装”，等待 Xcode Command Line Tools 安装完成。这里不需要安装完整的 Xcode。安装完成后关闭并重新打开终端，再次执行 `git --version`。

### 5.3 安装 Node.js 22 LTS

对初学者最简单的方式是使用 Node.js 官方安装包：

1. 打开 [Node.js 官方下载页](https://nodejs.org/en/download)。如果首页不再显示 Node.js 22，请进入 [Node.js Previous Releases](https://nodejs.org/en/about/previous-releases) 查找 22.x。
2. 选择 Node.js 22 LTS，版本必须是 `22.13.0` 或更高；为了复现当前验证环境，可以选择 `22.23.2`。
3. 优先下载官方 macOS Installer（`.pkg`）。如果页面提供架构选项，Apple Silicon 选择 ARM64，Intel Mac 选择 x64；如果只有一个 `.pkg`，直接使用官方提供的安装包。
4. 下载 `.pkg` 文件并按默认步骤安装。
5. 安装完成后关闭所有终端窗口，再重新打开终端。

Node.js 安装包会同时安装 npm，不需要使用 Homebrew，也不需要单独安装 npm。

### 5.4 安装代码编辑器（可选）

推荐从 [Visual Studio Code 官方网站](https://code.visualstudio.com/) 安装 VS Code。只想构建和使用现有代码时可以跳过。

### 5.5 验证环境

在新打开的终端中逐条执行：

```bash
git --version
node --version
npm --version
```

一个符合本文环境的示例结果是：

```text
git version 2.x.x
v22.23.2
10.9.8
```

小版本不必完全相同，但 Node.js 必须是 `v22.13.0` 或更高。如果出现 `command not found`，先确认软件已经安装，再关闭并重新打开终端。

### 5.6 克隆项目

只需在一台新电脑上克隆一次。逐条执行：

```bash
mkdir -p ~/Developer
cd ~/Developer
git clone https://github.com/caozichen/FillForm.git
cd FillForm
```

项目地址通常是：

```text
/Users/你的用户名/Developer/FillForm
```

确认当前位于正确的项目根目录：

```bash
pwd
test -f package.json && echo "已找到 package.json"
test -f package-lock.json && echo "已找到 package-lock.json"
```

后两条应分别输出“已找到”。如果项目已经存在，不要再次执行 `git clone`，直接用 `cd` 进入现有的 `FillForm` 文件夹。

### 5.7 安装依赖

确保终端当前路径是 `FillForm`，然后执行：

```bash
npm ci
```

`npm ci` 会严格按照 `package-lock.json` 安装依赖，在根目录创建 `node_modules`，并自动执行 `wxt prepare` 生成 `.wxt` 类型文件。成功时命令会正常结束并重新出现终端提示符，不应有 `npm ERR!`。

不要执行 `sudo npm ci`，也不要从 Windows 或另一种处理器架构的电脑复制 `node_modules`。依赖应在当前电脑上重新安装。

确认安装结果：

```bash
test -d node_modules && echo "依赖目录已生成"
test -f .wxt/tsconfig.json && echo "WXT 类型文件已生成"
```

### 5.8 运行基础检查

先运行两个不需要启动浏览器的检查：

```bash
npm run check:detect
npm run check:hashids
```

命令没有出现错误、最终回到终端提示符，就表示基础识别逻辑和 Hashids 功能通过检查。项目当前没有统一的 `npm test` 命令。

### 5.9 构建扩展

执行：

```bash
npm run build
```

该命令先让 WXT 生成生产产物 `.output/chrome-mv3`，再运行同步脚本，刷新稳定扩展目录 `extension/chrome-mv3`。成功时终端末尾应能看到类似内容：

```text
Synced unpacked extension to extension/chrome-mv3
```

验证并查看完整路径：

```bash
test -f extension/chrome-mv3/manifest.json && echo "扩展构建成功"
pwd
```

如果 `pwd` 输出 `/Users/你的用户名/Developer/FillForm`，Chrome 要加载的完整目录就是：

```text
/Users/你的用户名/Developer/FillForm/extension/chrome-mv3
```

### 5.10 在 Chrome 中加载扩展

1. 打开 Google Chrome。
2. 在地址栏输入 `chrome://extensions` 并按 Enter。
3. 打开页面右上角的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 在 macOS 文件夹选择窗口按 `Command + Shift + G`，打开“前往文件夹”。
6. 输入 `~/Developer/FillForm/extension/chrome-mv3` 并按 Enter。
7. 选择该文件夹并点击“打开”。
8. 扩展列表中出现“FillForm — 一键填充与二维码助手”，且没有红色错误，即表示加载成功。

必须选择直接包含 `manifest.json` 的 `extension/chrome-mv3`，不能选择项目根目录、`extension` 父目录或 ZIP 文件。`.output` 以点开头，在 macOS 中默认隐藏，而且只是临时构建目录；日常加载不需要进入它。

如果确实需要在 Finder 中查看隐藏文件，可以按 `Command + Shift + .` 切换显示状态；但加载 FillForm 时使用可见的 `extension/chrome-mv3` 更稳妥。

### 5.11 第一次验证插件

1. 打开一个普通的、允许扩展运行的表单网页。
2. 点击 Chrome 工具栏右侧的扩展图标，找到 FillForm，并可点击图钉固定。
3. 打开 FillForm 弹窗，尝试“识别字段”或“一键智能填充”。
4. 默认数据来源是本地 `heuristic`，不配置 API Key 也能运行。

Chrome 内置页面、Chrome 网上应用店、新标签页及其他禁止内容脚本注入的页面不能用于测试表单填充。这是 Chrome 的安全限制，不是安装失败。

### 5.12 macOS 开发模式

需要边修改边调试时，在项目根目录执行：

```bash
npm run dev
```

WXT 会生成 `.output/chrome-mv3-dev`，通常会打开一个独立的 Chrome/Chromium 开发窗口，并监听文件变化。运行期间终端会一直被占用，这是正常现象；按 `Control + C` 停止。

`npm run dev` 不会刷新仓库中的 `extension/chrome-mv3`。如果你在自己日常使用的 Chrome 中加载的是 `extension/chrome-mv3`，完成源码修改后仍应执行 `npm run build`，然后在 `chrome://extensions` 中点击 FillForm 卡片上的“重新加载”，最后刷新正在测试的网页。

### 5.13 macOS 更新到最新版代码

以后更新代码时，不要再次克隆。打开终端后执行：

```bash
cd ~/Developer/FillForm
git pull
npm ci
npm run build
```

然后回到 `chrome://extensions`，点击 FillForm 的“重新加载”，再刷新目标网页。如果本地有尚未提交的修改，`git pull` 可能提示冲突；应先提交或妥善保存自己的修改，不要强行删除。

## 6. 开发模式、生产构建和扩展目录的区别

| 目录或命令 | 用途 | 是否提交到 Git | 是否建议手动加载 |
| --- | --- | --- | --- |
| `npm run dev` | 启动 WXT 监听和开发浏览器。 | 不适用 | 通常由 WXT 自动加载到独立浏览器。 |
| `.output/chrome-mv3-dev` | 开发模式临时产物。 | 否 | 一般不需要手动加载。 |
| `.output/chrome-mv3` | 生产构建临时产物。 | 否 | 可以加载，但不是项目约定的稳定目录。 |
| `extension/chrome-mv3` | `npm run build` 同步后的正式未打包扩展目录。 | 是 | **是，日常 Chrome 应加载这里。** |
| `.output/formpilot-nova-wxt-3.1.0-chrome.zip` | 当前配置下 `npm run zip` 生成的压缩包；后续版本号变化时文件名也会变化。 | 否 | 不能直接加载，复制到其他电脑后先解压。 |

`scripts/sync-extension.mjs` 在每次 `npm run build` 时会删除并重新创建 `extension/chrome-mv3`。因此不要直接长期修改该目录里的生成文件，否则下次构建会覆盖这些修改。应该修改 `public/`、`legacy/`、`entrypoints/`、`wxt.config.ts` 等源码。

## 7. 项目主要目录

| 路径 | 说明 |
| --- | --- |
| `entrypoints/` | WXT 编译入口，包括后台 Service Worker 和离屏条码入口。 |
| `public/` | 构建时复制进扩展的页面、内容脚本、适配器、样式和图标。 |
| `legacy/` | 当前仍参与构建的后台运行时源码，不是无用的历史备份。 |
| `scripts/` | 构建同步、字段识别、Hashids、级联和条码回归脚本。 |
| `extension/chrome-mv3/` | 已构建并提交到 Git 的 Chrome 扩展目录。 |
| `.output/` | WXT 的临时开发、生产和 ZIP 产物，Git 会忽略。 |
| `.wxt/` | WXT 自动生成的类型和缓存，Git 会忽略。 |
| `node_modules/` | npm 安装的依赖，Git 会忽略。 |
| `package.json` | 直接依赖和可执行命令。 |
| `package-lock.json` | 当前完整依赖树的精确版本，必须和 `package.json` 一起保留。 |
| `wxt.config.ts` | 扩展名称、版本、权限、内容脚本和 Manifest 配置。 |

## 8. 常用命令

所有命令都在项目根目录执行，Windows PowerShell 和 macOS 终端使用相同的 npm 命令。

| 命令 | 作用 | 主要前置条件 |
| --- | --- | --- |
| `npm ci` | 按锁文件精确安装依赖，并运行 `wxt prepare`。 | Node.js 22.13+、可访问 npm registry。 |
| `npm run dev` | 启动 WXT 开发模式和文件监听。 | 已安装依赖，本机已安装 Chrome/Chromium。 |
| `npm run build` | 生产构建，并刷新 `extension/chrome-mv3`。 | 已安装依赖。 |
| `npm run check:detect` | 检查字段识别、数据生成和填充辅助逻辑。 | 已安装依赖。 |
| `npm run check:hashids` | 检查 Hashids 编码、解码和错误输入。 | 已安装依赖。 |
| `npm run check:cascader-address` | 用本地测试页面和 Chrome 检查级联、地址组件。 | Node.js 22.13+、本机 Chrome。 |
| `npm run check:framework-cascader` | 用本地测试页面和 Chrome 检查 Arco/Element 级联路由。 | Node.js 22.13+、本机 Chrome。 |
| `npm run zip` | 重新执行生产构建，并在 `.output/` 中生成名称以 `-chrome.zip` 结尾的压缩包。 | 已安装依赖。 |

建议提交代码前至少执行：

```bash
npm run check:detect
npm run check:hashids
npm run check:cascader-address
npm run check:framework-cascader
npm run build
```

两个级联检查会在后台临时启动无界面的 Chrome 进程，测试结束后自动关闭。以终端中的通过或失败输出为准；任务管理器或活动监视器中短暂出现 Chrome 进程属于正常现象。项目目前没有统一的 `npm test`、lint 或 typecheck 命令。

如果要把扩展发给其他电脑，建议执行：

```bash
npm run build
npm run zip
```

当前扩展配置会生成 `.output/formpilot-nova-wxt-3.1.0-chrome.zip`。后续扩展版本变化时文件名也会变化，应以 `npm run zip` 末尾显示的 `Zipped extension` 路径为准。

Windows PowerShell 可以用下面的命令查找刚生成的最新 Chrome ZIP：

```powershell
Get-ChildItem .\.output\*-chrome.zip | Sort-Object LastWriteTime -Descending | Select-Object -First 1 -ExpandProperty FullName
```

macOS 可以使用：

```bash
ls -t .output/*-chrome.zip | head -n 1
```

在另一台电脑上先完整解压 ZIP，再让 Chrome 加载解压后直接包含 `manifest.json` 的文件夹。`npm run zip` 本身不会刷新 `extension/chrome-mv3`，所以需要稳定扩展目录和 ZIP 都保持最新时，先执行 `npm run build`。

## 9. 修改代码后的标准流程

在 VS Code 中打开整个 `FillForm` 文件夹，不要只打开某一个生成文件。一次普通修改建议按下面顺序完成：

1. 修改 `public/`、`legacy/`、`entrypoints/` 或配置文件中的源码。
2. 保存文件。
3. 运行与修改相关的检查脚本。
4. 执行 `npm run build`。
5. 打开 `chrome://extensions`，点击 FillForm 的“重新加载”。
6. 刷新目标表单网页，然后重新测试。

只刷新目标网页、不重新构建和重新加载扩展，Chrome 仍可能运行旧代码。

## 10. 云端模型和 API Key

第一次运行请保持默认的 `heuristic` 数据来源，它不需要账号、API Key 或网络模型服务。

需要使用 DeepSeek、OpenAI 或智谱时，在 FillForm 的设置中心填写对应的 Base URL、模型名称和 API Key。这些配置：

- 不写入 `.env`。
- 保存在当前浏览器配置的 `chrome.storage.local`。
- 不会随 Git、扩展目录或 ZIP 自动同步到另一台电脑。
- 不应使用共享账号中的生产密钥或真实敏感数据。

云端模式会把必要的字段描述和候选信息发送给所配置的服务，使用前应确认测试环境的数据合规要求。

## 11. 常见问题与处理方法

### 11.1 `node`、`npm` 或 `git` 找不到

- 安装软件后关闭并重新打开 PowerShell 或终端。
- Windows 重新运行安装程序，确认加入 PATH。
- macOS 执行 `xcode-select --install` 安装 Git；Node.js 使用官方 `.pkg` 重新安装。
- 重新运行 `git --version`、`node --version` 和 `npm --version` 确认。

### 11.2 Windows 报 `npm.ps1 cannot be loaded because running scripts is disabled`

不必为了本项目修改全局 PowerShell 安全策略。改用 npm 的 `.cmd` 入口：

```powershell
npm.cmd ci
npm.cmd run build
```

其他命令同理，例如 `npm.cmd run dev`。

### 11.3 出现 `EBADENGINE` 或 Node 版本不支持

执行：

```bash
node --version
```

如果低于 `v22.13.0`，卸载旧版本并安装 Node.js 22 LTS 的较新版本，然后重新打开终端并执行 `npm ci`。不要用 `--force` 绕过版本检查，因为构建或浏览器回归脚本仍可能失败。

### 11.4 提示找不到 `package.json`

说明当前终端不在项目根目录。

Windows：

```powershell
Set-Location "$HOME\source\FillForm"
Test-Path .\package.json
```

macOS：

```bash
cd ~/Developer/FillForm
test -f package.json && echo "目录正确"
```

### 11.5 `npm ci` 出现网络错误

`ECONNRESET`、`ETIMEDOUT` 或 `ENOTFOUND` 通常表示网络、代理或 DNS 无法访问 npm 官方源。先检查浏览器是否能打开 [npm 官方仓库](https://registry.npmjs.org/)，然后执行：

```bash
npm cache verify
npm ci
```

`.npmrc` 当前没有配置私有源。不要随意使用来路不明的镜像，也不建议用 `--force` 忽略安装错误。

### 11.6 macOS 出现 `EACCES` 权限错误

不要使用 `sudo npm ci`。先查看错误信息中的具体路径，再只修复对应目录：

- 如果错误路径位于当前用户的 `~/.npm`，说明 npm 缓存可能曾被 `sudo npm` 写成 root 所有。执行下面三条命令；输入 macOS 密码时终端不会显示字符，这是正常现象。

```bash
sudo chown -R "$(id -u)":"$(id -g)" "$HOME/.npm"
npm cache verify
npm ci
```

- 如果错误路径位于本文示例项目 `~/Developer/FillForm`，并且该项目确实属于 root，可以只修复这个明确的项目目录：

```bash
sudo chown -R "$(id -u)":"$(id -g)" "$HOME/Developer/FillForm"
npm ci
```

不要把 `chown -R` 用在 `$HOME`、`/Users` 或系统根目录。重新安装 Node.js 本身不会修复已经归 root 所有的缓存或项目文件。

### 11.7 Chrome 提示“清单文件缺失”或“无法加载清单”

通常是选错了文件夹。确认以下文件存在：

```text
FillForm/extension/chrome-mv3/manifest.json
```

Chrome 应选择 `chrome-mv3` 这一层。不要选择项目根目录、`extension` 父目录或 ZIP 文件。如果文件确实不存在，在项目根目录执行 `npm run build`。

### 11.8 macOS 文件选择器看不到隐藏目录

`.output` 和 `.wxt` 以点开头，因此默认隐藏。按 `Command + Shift + .` 可以显示隐藏文件，按 `Command + Shift + G` 可以直接输入路径。

但是 FillForm 正常加载目录 `extension/chrome-mv3` 并不是隐藏目录。最简单的做法是在“前往文件夹”中输入：

```text
~/Developer/FillForm/extension/chrome-mv3
```

### 11.9 修改代码后扩展没有变化

依次完成：

1. 在项目根目录执行 `npm run build`。
2. 在 `chrome://extensions` 点击 FillForm 的“重新加载”。
3. 刷新正在测试的网页。

如果你正在使用 `npm run dev` 自动打开的独立开发浏览器，就在那个浏览器里测试；不要和日常 Chrome 中加载的生产目录混淆。

### 11.10 `npm run dev` 无法打开浏览器或端口被占用

先确认 Chrome 安装在系统默认位置。开发模式只是便利工具，不影响正式构建；可以停止它，执行 `npm run build`，再按本文步骤手动加载 `extension/chrome-mv3`。

需要换开发服务器端口时可以执行：

```bash
npm run dev -- --port 3001
```

### 11.11 普通网页可以用，但 `chrome://` 页面没有反应

Chrome 禁止扩展内容脚本注入部分内置页面、Chrome 网上应用店和新标签页，这是正常限制。请在普通的 `http://` 或 `https://` 表单网页上验证。

### 11.12 Chrome 提示扩展可以读取所有网站数据

项目 Manifest 声明了 `<all_urls>`，用于在普通网页中识别和填写表单，也用于按配置访问 API。这是当前功能设计对应的预期权限提示。只应在你信任的源码和测试环境中加载未打包扩展。

### 11.13 换电脑后设置和测试数据不见了

源码和 `extension/chrome-mv3` 会通过 Git 同步，但扩展设置、测试数据、模板、API Key 和已保存文件位于每台电脑自己的 `chrome.storage.local`，不会通过 Git 自动迁移。

### 11.14 依赖目录损坏或从另一种系统复制过来

不要修补单个依赖，也不要复制其他电脑的 `node_modules`。回到项目根目录重新执行：

```bash
npm ci
```

`npm ci` 会按照锁文件重新创建依赖树。若仍失败，先确认 Node.js 版本和网络，再查看终端中第一条实际错误，而不是只看最后一行。

## 12. 最终验收清单

完成环境搭建后，逐项确认：

- `git --version` 能显示 Git 2.x。
- `node --version` 显示 Node.js 22，且不低于 `22.13.0`。
- `npm --version` 能正常显示版本，推荐 10.x。
- 项目根目录中存在 `package.json` 和 `package-lock.json`。
- `npm ci` 成功，已生成 `node_modules` 和 `.wxt`。
- `npm run check:detect` 和 `npm run check:hashids` 成功。
- `npm run build` 成功，并显示同步到 `extension/chrome-mv3`。
- `extension/chrome-mv3/manifest.json` 存在。
- Chrome 加载的是 `extension/chrome-mv3`，不是项目根目录或 ZIP。
- `chrome://extensions` 中 FillForm 没有红色错误。
- 在普通表单网页上可以打开 FillForm，并执行字段识别。

以上全部满足，就表示这台电脑已经可以开发、构建和运行 FillForm。
